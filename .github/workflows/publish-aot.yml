name: Publish Native AOT Release

on:
  release:
    types: [published] 

jobs:
  publish:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [windows-latest, ubuntu-latest]
        include:
          - os: windows-latest
            runtime: win-x64
            asset_name: your-app-win-x64.zip
          - os: ubuntu-latest
            runtime: linux-x64
            asset_name: your-app-linux-x64.zip

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '9.0.x'

      - name: Publish Native AOT
        run: dotnet publish -p:PublishAot=true -r ${{ matrix.runtime }} -c Release

      - name: Zip artifacts
        shell: bash
        run: |
          cd ./bin/Release/net8.0/${{ matrix.runtime }}/publish
          zip -r ../../../${{ matrix.asset_name }} .

      - name: Upload to Release
        uses: softprops/action-gh-release@v2
        with:
          files: ./bin/Release/net8.0/${{ matrix.asset_name }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
